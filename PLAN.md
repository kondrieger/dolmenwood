# План доработок — Кузница персонажей Dolmenwood

Файл-навигатор для работы по шагам. Если сессия прервалась — открыть этот файл,
найти первый невыполненный пункт и продолжить с него. Отмечать сделанное `[x]`.

Составлен: 11 августа 2026.

---

## Два закона этой работы

**Закон 1. Ничего нельзя придумывать.**
Ни одного предмета, числа, веса, цены, черты, таблицы или правила, которых нет в книге.
Если чего-то в книге нет — этого нет и в приложении. Не «примерно как в OSE», не «логично
было бы», не «в других редакциях так». Нет в книге — значит нет.

**Закон 2. Абсолютный источник истины — Player's Book.**
Dolmenwood Player's Book, издание от 6 августа 2024, 201 страница.

- локально: `C:\Users\kondr\Downloads\Dolmenwood_Player_s_Book-1.pdf` (38 269 062 байта)
- Google Drive: https://drive.google.com/file/d/1jHY5Duf0uSozYkiNxG4gmg2OqBN4smYK/view
  прямая загрузка: `https://drive.google.com/uc?export=download&id=1jHY5Duf0uSozYkiNxG4gmg2OqBN4smYK`

**Как её читать:** инструмент Read не открывает этот PDF (не установлен poppler).
Текст извлекать через **pypdf**:

```bash
python -c "import pypdf; r=pypdf.PdfReader(PATH); print(r.pages[N].extract_text())"
```

**Осторожно со страницами: смещение НЕ постоянное.** В начале книги страница PDF
на 1 больше печатной, а к шестой части — уже на 2, потому что разделители частей
сбивают счёт. Не вычислять номер страницы формулой — искать таблицу по тексту
во всём документе. Печатный номер страницы виден в самом тексте, обычно первой
строкой после колонтитула.

Ключевые разделы (нумерация книги): роды 28–56, классы 57–75, магия 76–113,
снаряжение/услуги/животные 115–135, приключения 136–172, приложения 173–189
(род-классы 180–189).

**Единственное законное исключение** — художественный стиль промпта для картинки
(палитра, освещение, композиция). Это не правило игры, а оформление. Всё, что промпт
*утверждает о персонаже*, обязано быть из книги или из брошенных кубиков.

**Правило маркировки.** Всё, что пользователь ввёл руками и чего нет в книге
(произвольный предмет, отредактированный вес), помечается флагом `custom: true`
и видимо отличается в интерфейсе — чтобы Рефери сразу видел отсебятину.

---

## Что уже проверено и сходится с книгой

Не перепроверять без повода, это сделано 11.08.2026 сверкой с PDF:

- девять классов: опыт, кости хитов, атака, все пять спасбросков, заклинания в день
  (клирик, монах, маг), цели навыков (бард 4, охотник 4, вор 8, чароплёт, маг),
  боевые таланты воина по уровням, броня веры монаха;
- пять род-классов: опыт, атака, спасброски, гламуры по уровням, кость хитов и прибавка после 10;
- различие «Безумного веселья» вудгрю: род — раз в день, род-класс — раз в день **за уровень**;
- оружие (20), броня (7 + щит), боеприпасы, свет, лагерь, святые предметы,
  инструменты, одежда — в `data/equipment.js` полностью, до строки.

---

## Порядок работы

Сначала дешёвое и безопасное (3, 4), потом данные (1), потом интерфейс (5, 6),
потом анимация (2), потом вход (7), потом промпт (8). Каждый пункт — отдельный коммит.

---

## [x] 1. Каталог: все предметы книги ✅ сделано 11.08.2026

**Проблема.** В каталоге только оружие, броня, боеприпасы и приключенческое снаряжение
(книга, стр. 116–119). Всё остальное, что продаётся и носится, отсутствует.

**Источник:** стр. 116–135.

### 1.1 Дополнить `data/equipment.js`

Ёмкости, которых нет (стр. 117), — цена в зм, вес в монетах:

| Предмет | Вместимость | Цена | Вес |
|---|---|---|---|
| Бочка / Barrel | 320 пинт | 1 | 70 |
| Ведро / Bucket | 40 пинт | 1 | 20 |
| Ларец железный большой / Casket (iron, large) | 800 монет | 30 | 400 |
| Ларец железный малый / Casket (iron, small) | 250 монет | 10 | 100 |
| Сундук деревянный большой / Chest (wooden, large) | 1000 монет | 5 | 200 |
| Сундук деревянный малый / Chest (wooden, small) | 300 монет | 1 | 50 |

Новые массивы (каждый — со страницей книги в комментарии):

- `PIPES` — 4 трубки (стр. 128): болотный дуб 15, вишня 5, глина 1, тыква-горлянка
  (мослингская) 2. **Вес любой трубки 10 монет.**
- `PIPELEAF` — 20 сортов (стр. 128–130): цена за одну закурку, доступность
  (Всегда / 3 из 6 / 1 из 6), описание и эффект. **Вес одной закурки 1 монета.**
- `HERBS` — 20 грибов и трав (стр. 130–131): цена, тип (растение/гриб), доступность,
  краткий эффект и полное описание. **Вес одной дозы 4 монеты.**
- `BEVERAGES` — 8 обычных, 20 необычных, 12 редких (стр. 126–127): тип, цена, эффект.
  **Бутыль 30 монет (5 порций по цене 4), бочонок 80 монет (10 порций по цене 8).**
- `ANIMAL_GEAR` — сбруя и корм (стр. 121, 123): корм лошади 5 мм/день вес 100,
  барда +2 КБ 150 зм вес 600, вьючное седло 10 зм вес 150, верховое седло 25 зм вес 300,
  седельные сумки 5 зм вес 100 (вмещают 500 монет), собачья броня +2 КБ 25 зм вес 200,
  корм собаке 1 зм вес 20.
- `HORSES` — 6 пород (стр. 120) со статблоками и грузоподъёмностью.
- `HOUNDS` — 8 пород (стр. 122–123) со статблоками.
- `VEHICLES` — повозки и суда (стр. 121).
- `SPECIAL_METALS` — холодное железо (двойная цена, +1 урона феям и полуфейским,
  −1 остальным) и серебро (десятикратная цена), срок изготовления 2d6 дней (стр. 119).

Мелочи, которых тоже нет:

- поштучно: стрела 25 мм, болт 5 см (стр. 118);
- монеты: пеллюцидий (pp) = 5 зм = 50 см = 500 мм (стр. 117);
- флаг `townOnly: true` — в книге такие предметы набраны курсивом и продаются
  только в городах (стр. 117);
- подгонка брони: изготовление по размеру 2d6 дней без наценки, переделка чужой
  брони 2d6 дней и половина цены (стр. 119);
- штраф за запрещённое роду/классу: оружие −4 к атаке, броня −2 КБ и отключение
  способностей, завязанных на скрытность, реакцию, ловкость и магию (стр. 119,
  необязательное правило).

### 1.2 Расширить `itemCatalogue()` в `utils/sheet.js`

- новые разделы: `Трубки`, `Трубочный лист`, `Грибы и травы`, `Напитки`,
  `Сбруя и корм`, `Имущество` (лошади, гончие, повозки);
- у раздела `Имущество` — флаг `carried: false`: в вес и слоты не идёт,
  живёт отдельным списком «Имущество и животные»;
- в строку поиска добавить название раздела и ключевые слова, чтобы предмет
  находился не только по имени.

### 1.3 Что вышло

**Новый файл `data/goods.js`** — трубки, лист, травы, напитки, сбруя, лошади,
гончие, повозки. В конце `data/equipment.js` носимое из него подмешивается
в `GEAR` через `Object.assign`, поэтому каталог, инвентарь и подсчёт веса
работают с новыми товарами как со всем прочим, без правок.

**Цены переехали в медяки.** Половина рынка стоит меньше золотого, а дроби от
золота дают ошибки округления. Внутри всё целое в `cp` (1 зм = 10 см = 100 мм),
показывается через `utils/money.js`: «9 мм», «5 см», «3 зм 5 см».
У старых записей остался `cost` в золотых — `priceCp()` понимает оба вида.

**Напитки — по две-три позиции на каждый.** Веса одной порции в книге НЕТ,
поэтому у порции `weight: null` и прочерк в таблице; выдумывать нельзя.
Вес тары в книге есть, поэтому бутыль (30 монет, 5 порций по цене 4) и бочонок
(80 монет, 10 порций по цене 8) — отдельные позиции с настоящим весом.
Бутылями идут вина, крепкое и особые, бочонками — пиво и сидр.

**Имущество отделено от поклажи.** Лошади, гончие и повозки живут в
`ch.equipment.property`, в вес и слоты не идут — их несёт не персонаж.
В листе для них отдельная таблица со статблоками и грузоподъёмностью.

**Итог: 235 позиций в каталоге.** Оружие 20 · Броня 7 · Ёмкости 12 · Свет 6 ·
Лагерь 7 · Святое 4 · Инструменты 32 · Одежда 6 · Боеприпасы 5 · Трубки 4 ·
Трубочный лист 20 · Грибы и травы 20 · Сбруя и корм 7 · Напитки 63 (33 напитка
и 30 видов тары) · Лошади 6 · Гончие 8 · Повозки и суда 8.

Напитков в книге именно **33**, а не 40, как я написал в первой редакции плана:
8 обычных, 16 необычных, 9 редких — по описаниям это 8 пива и сидра, 4 особых,
12 крепких, 3 чая и 6 вин.

### 1.4 Проверка

`scratchpad/verify_goods.mjs` разбирает таблицы из текста PDF и требует, чтобы
каждое название, цена, вес, тип и доступность в наших данных нашлись в книге.
**401 проверка, расхождений нет.**

`scratchpad/t_goods_ui.mjs`: 53 листа генерируются, все **235** позиций каталога
добавляются без ошибки, имущество не меняет нагрузку, порция без веса не ломает
подсчёт, трава весит 4, закурка 1, бутыль 30.

Важно на будущее: **номер страницы нельзя вычислять формулой** — смещение
печатной страницы относительно PDF в книге не постоянно. Первая версия сверки
из-за этого «не нашла» вообще ничего. Искать по всему тексту.

---

## [x] 2. Кубики: почему ломаются и что чинить ✅ сделано 11.08.2026

**Файл:** `components/dice/DiceTray.vue`.

**Причина 1 — главная. Утечка интервалов.**
В `onMounted` для каждой группы создаётся `setInterval(spin, 55)`, а в
`onBeforeUnmount` вызывается только `timers.forEach(clearTimeout)` — интервалы
**не гасятся**. При перегенерации `shownSteps.value = []` размонтирует все лотки,
но их интервалы продолжают крутиться вечно и дёргать `faces` мёртвого компонента.
За несколько перебросов накапливаются десятки живых таймеров — отсюда рывки и
«зависшие» кубики.
*Чинить:* складывать интервалы в тот же массив и гасить их; регистрировать
`onBeforeUnmount` на верхнем уровне `setup`, а не внутри `onMounted`.

**Причина 2. d100 показывает не то.**
`rollD100()` возвращает `dice: [tens, ones]`, где обе цифры 0–9 (`utils/dice.js:44`).
Во время кручения лоток рисует `randInt(10) + 1` — то есть 1–10. Цифра прыгает в
одном диапазоне, а замирает в другом.
*Чинить:* кость десятков рисовать как `00, 10, … 90`, кость единиц как `0–9`,
и крутить их в тех же диапазонах.

**Причина 3.** `g.value` без значения печатает `—` вместо суммы.

**Причина 4 — нашлась по ходу, самая скверная.** В шаге лунного знака
(`utils/generator.js`) лоток брал `log.entries[log.entries.length - 1]`, то есть
**последнюю запись журнала**. Но между броском луны и этой строкой успевает
отработать `birthdayFromMoon`, которая пишет в журнал свой бросок дня рождения.
В итоге в лотке с подписью «луна» показывался **чужой бросок** — на скриншоте
это выглядело как «100 десятки / ? единицы / 10». Само значение луны при этом
было верным: врал только показ.
*Починено:* `pickRange` отдаёт свой бросок, его и показываем; `birthdayFromMoon`
возвращает `roll`, и день фазы показывается отдельной костью со своей подписью.

**Причина 5.** При «4d6, отбросить худшую» показывались четыре кости и сумма,
которая им не равна, без всякого объяснения. Теперь генератор передаёт
`dropIndex`, отброшенная кость гаснет и подписывается «отброшена».

**Сделано в анимации** (внешние ассеты не подключались — всё офлайн):
силуэты костей через `clip-path` уже были в `main.css`; добавлены разное время
кувырка у каждой кости через переменную `--spin` (в унисон они кувыркаются
неживо), приземление вразнобой (`360 + группа × 90 + кость × 70` мс) и
поддержка `prefers-reduced-motion` — и в CSS, и проверкой в компоненте.
Кнопка «показать сразу» теперь гасит анимацию и у лотков, которые уже крутятся:
раньше она влияла только на ещё не созданные.

**`utils/dice.js` не тронут.** Броски честные (crypto с отбраковкой), это
гарантия перед Рефери. Чинилось только отображение.

### Проверка

`scratchpad/t_dice_honest.mjs` — отвечает на прямой вопрос «совпадают ли
видимые кубики с реально брошенными». Для каждой группы костей в каждом шаге:
грань возможна на такой кости, число костей совпадает с тем, что нарисует лоток,
у d100 ровно два разряда 0–9 и итог сходится, при «отбросить худшую» гаснет
именно минимальная кость и итог равен сумме оставшихся, и главное — показанный
бросок **находится в журнале** как настоящий.
**318 листов, 10 249 групп, 16 249 костей, 69 042 проверки — расхождений нет.**

Утечку таймеров подтвердил временным логом в `stopAll` (потом убран): при уходе
со страницы во время броска гасится 36 таймеров — 18 костей × интервал и таймаут.
Раньше 18 интервалов оставались крутиться после размонтирования.

Замерить утечку из браузерного инструмента не вышло: его JS живёт в отдельной
области видимости — DOM общий, а `window` свой, поэтому подмена `setInterval`
не видит вызовов страницы. Если понадобится снова — инструментировать изнутри
компонента и читать консоль.

---

## [x] 3. Поиск: ё и е — одна буква ✅ сделано 11.08.2026

**Причина, почему «некоторые предметы не ищутся».** Поиск в
`components/character/ItemCatalog.vue:27` сравнивает строки как есть. В названиях
много «ё»: вер**ё**вка, кош**е**ль, ём**к**ости, флакон масла, шиш**е**чный доспех.
Пользователь набирает «веревка» — совпадения нет.

**Сделано.** Заведён `utils/text.js`: `normalize()` (нижний регистр, ё → е, схлопнутые
пробелы) и `matches()` — режет запрос на слова и требует, чтобы нашлись все,
поэтому «верев 50» находит верёвку в 50 футов, а порядок слов не важен.
`ItemCatalog.vue` ищет через `matches` и теперь смотрит ещё и на название раздела,
так что «броня» и «свет» — тоже рабочие запросы.

В каталоге оказалось **6 предметов с «ё»** — они и не находились.
Проверка перебирает все шесть: каждый обязан найтись, если набрать его через «е».

Справочник `pages/rules.vue` поля поиска не имеет — там чинить нечего.
Применить `matches` в диалоге предмета, когда он появится (пункт 6).

---

## [x] 4. Оружие не кладётся в рюкзак ✅ сделано 11.08.2026

**Это баг, а не правило.** В `utils/sheet.js:142` ветка `row.kind === 'weapon'`
всегда делает `eq.weapons.push(...)`, полностью игнорируя аргумент `where`.

Книга запрета не содержит: нагрузка по весу (стр. 148) считает всё, что персонаж
несёт, а слотовая нагрузка (стр. 149) прямо разделяет «на себе» и «в рюкзаке».
Запасной меч в мешке — обычное дело.

**Сделано.**

- `addItem` уважает `where`: при `stowed` кладёт оружие в `eq.stowed` записью
  с `kind: 'weapon'`, сохраняя урон, размер и качества; одинаковые складываются
  в стопку через общий помощник `stackInto`;
- `moveItem` понимает третий источник `'weapons'` и водит оружие между руками
  и рюкзаком в обе стороны. Оружие ходит между `eq.weapons` и `eq.stowed` минуя
  `eq.equipped`, потому что атаки берутся именно из `eq.weapons` — значит убранное
  в рюкзак само выпадает из списка атак, а вес остаётся;
- `InventoryPanel.vue`: у оружия в руках появилась кнопка ↓ «убрать в рюкзак»,
  в рюкзаке оружие показывает урон и подпись «убрано, для атаки надо достать»,
  кнопка ↑ подписана «взять в руки». Заодно исправлен возможный конфликт ключей
  в списке рюкзака: `holywater` есть и в оружии, и в снаряжении;
- `computeLoad` и `countSlots` в `utils/generator.js` считают убранное оружие
  без правок — они уже обходили `eq.stowed` целиком.

**Проверено** скриптом (вес сходится по кругу: положил → достал → убрал → удалил,
возврат к исходному числу) и в браузере на листе Шмолда: длинный меч лёг в рюкзак,
нагрузка выросла с 1021 до 1051 ровно на вес меча (30). Тестовая правка откачена,
файл Шмолда чист.

---

## [x] 5. Безделушка при ручном вводе ✅ сделано 11.08.2026

**Причина.** `utils/manual.js:347` жёстко пишет `trinket: { ru: '—', en: '—', roll: null }`.
При генерации безделушка бросается по d100 из таблицы рода (`utils/generator.js:656`),
а при ручном вводе поля просто нет.

**Сделано в `utils/manual.js`:**

- `emptyDraft()` — появилось `trinket: { ru: '', en: '', roll: null }`;
- `buildCharacter()` — пишет выбранное вместо жёсткого прочерка, сохраняя номер броска;
- `checkDraft()` — при пустой безделушке замечание уровня `info`, не ошибка:
  персонаж мог её потерять, продать или проесть, и создание это не блокирует.

**Сделано в `pages/manual.vue`:** блок «Безделушка» с тремя путями — выбрать из
таблицы рода (50 строк с диапазонами d100 и поиском через `matches`, то есть
с ё/е), бросить честную d100 кнопкой, или вписать свою. Выбранная показывается
карточкой с номером броска и пометкой «своя, в таблице рода такой нет».
При смене рода таблица и выбор сбрасываются — безделушки у родов разные.

**Заодно исправлена настоящая пропажа.** Форма спрашивала «Тело / Шерсть» под
общим ключом `body`, а `buildCharacter` шёл по ключам рода — у бреггла и
гримолкина это `fur`. В итоге у них **примета внешности просто терялась** при
сборке листа. Теперь и форма, и сборка идут по `kin.details` конкретного рода,
и подписи берутся оттуда же, без второго списка, который мог разойтись.

**Проверено** (`scratchpad/t_trinket.mjs`, 61 проверка): поле есть в заготовке;
пустая безделушка даёт info и не мешает сохранению; выбранная, брошенная (с
номером) и своя доезжают до листа; у всех шести родов ровно 50 диапазонов,
покрывающих d100 без дыр и нахлёстов; примета из таблицы рода собирается для
каждого рода, включая `fur` у бреггла и гримолкина, с правильной подписью;
для каждого рода собирается полный набор примет.

В браузере: бросок дал 71 → «Статуэтка единорога» (диапазон 71–72 — сходится),
замечание о пустой безделушке исчезло; переключение на бреггла сменило «Тело»
на «Шерсть», подставило его таблицу безделушек и сбросило выбор.

---

## [x] 6. Карточка предмета и произвольный предмет ✅ сделано 11.08.2026

**Новый компонент `components/character/ItemDialog.vue`** — один диалог на два
дела: правка предмета и создание своего.

Открывается щелчком по названию в любой строке инвентаря: броня, щит, оружие
в руках, снаряжение на себе и в рюкзаке, имущество. Сверху — карточка
**«Как в книге»** с исходными названием, ценой, весом, уроном и размером,
классом брони и громоздкостью, описанием и статблоком для животных.

Правятся название (рус. и англ.), количество, вес, цена, слоты, урон у оружия,
заметка и **где предмет лежит**. Перенос через карточку понимает, что у оружия
свой маршрут: «в руках» и «в рюкзаке», без «на себе», потому что атаки берутся
из списка в руках.

**Отсебятина всегда видна.** Если книжное значение изменили, предмет получает
`edited` и метку «ПРАВЛЕНО» в списке, а в карточке появляется строка вида
«Вес — в книге 20, у нас 50» и кнопка **«Вернуть как в книге»**. Расхождения
считаются сравнением с живой записью книги (`bookEntry`), а не снимком,
так что снимок не может протухнуть.

**Свой предмет** заводится кнопкой «✎ Свой предмет»: название, вид (снаряжение
или оружие), где лежит, количество, вес, цена, слоты, урон и размер для оружия,
описание. Получает `custom: true`, идентификатор `custom_<имя>_<случайное>`
и метку «СВОЁ». Такой предмет с книгой не сравнивается — сравнивать не с чем.

Новые операции в `utils/sheet.js`: `itemAt`, `bookEntry`, `divergences`,
`updateItem`, `resetToBook`, `relocateItem`, `addCustomItem`. Все зовут
`recompute()`, поэтому вес, скорость и нагрузка пересчитываются сразу.

**Проверено** (`scratchpad/t_itemdialog.mjs`, 34 проверки): книжная запись
находится для оружия, брони, щита, снаряжения и имущества; правка веса меняет
нагрузку и ставит метку, возврат к книге снимает и то и другое; количество не
опускается ниже 1; перенос не меняет вес; свой предмет не сравнивается с книгой
и не получает метку «правлено»; 200 своих предметов подряд дают разные
идентификаторы; **правка и возврат прогнаны по всем 235 позициям каталога**.

В браузере на листе Шмолда: 14 кликабельных предметов, карточка короткого меча
показала книжные 7 зм / 20 монет / 1d6, правка веса на 50 подняла нагрузку
1021 → 1051 и повесила «ПРАВЛЕНО», «Вернуть как в книге» откатило и то и другое.
Крукхорнское копьё Шмолда само подсветилось как «СВОЁ» — его в книге нет.
Тестовые правки откачены.

---

## [x] Правки по ходу: пустые «Особые дары» ✅ 11.08.2026

Вопрос от игрока: у вудгрю и бреггла карточка «Особые дары» пуста — так и надо?

**Вудгрю — да.** Все его дары автоматические: Безумное веселье, Лунное зрение,
защитный бонус, Неудержимое ликование, инструмент как оружие, уязвимость к
холодному железу, Скрытность (стр. 188–189). Выбирать не из чего.
**Пустая карточка теперь не показывается вовсе** — она только сбивала с толку.

**Бреггл — нет, была дыра.** На 1–3 уровне тоже нечего выбирать, но с 4 уровня
книга даёт ему тайные заклинания (стр. 181), а **таблицы заклинаний в день у нас
не было вообще**. Добавлена и сверена с книгой.

Вдобавок нашлась причина, по которой она бы всё равно не заработала:
в `buildProfile` у род-классов `magicType` был захардкожен в `null`, а
`spellsPerDay` не копировался. Тот же класс бага, что раньше был с `glamoursByLevel`.

Бреггл не получает стартовой книги заклинаний (книга прямо говорит: учит у
наставника, из найденных книг или исследованием), поэтому в форме появился блок
«Выученные тайные заклинания», а на листе — панель Arcane Spells.

### Сплошная сверка страницы ручного ввода

`scratchpad/t_manual_coverage.mjs` отвечает на вопрос «для всех ли родов и классов
можно указать всё нужное». Для каждого из **53 сочетаний** на **каждом из 15
уровней** сверяются два списка: что дают правила (выводится из данных профиля и
рода) и что показывает форма (`giftBlocks` из `utils/manual.js` — тот же список,
которым рисуется страница). Ошибкой считается и пропуск, и лишний блок.
Плюс проверка, что каждый выбор доезжает до листа: гламуры и руны эльфа-чароплёта,
сноровка и плоть мослинга, заклинания и слоты бреггла, таланты воина, орден
клирика, сюзерен рыцаря, книга мага. **2100 проверок — пробелов нет.**

Чтобы сверка не была самообманом, список блоков вынесен из страницы в `manual.js`:
форма и проверка читают его из одного места.

---

## [ ] 7. Вход по выбору игрока и журнал правок

### 7.1 Вход

**Новый композабл `composables/useAuth.ts`.** В `localStorage`: `dw-player` — выбранное
имя из `data/players.js`, и `dw-device` — случайный идентификатор браузера, который
создаётся один раз и **не меняется при смене имени игрока** (см. 7.3).
Наружу: `player`, `deviceId`, `signedIn`, `canEdit(ch)`, `signIn(name)`, `signOut()`.

**`components/layout/AppHeader.vue`** — селект с семью игроками и пунктом «Выйти».
Пока не выбран — крупная плашка «Выберите игрока, чтобы редактировать».

**Пока не выбран игрок — блокируется всё, что меняет данные:** сохранение из
генератора, ручное создание, правка листа, добавление и удаление предметов,
повышение уровня, смена статуса, удаление персонажа. Просмотр остаётся свободным.

Блокировать не только видом, но и `disabled` на кнопках, и проверкой в обработчиках,
и проверкой на сервере в `[id].put.ts` и `[id].delete.ts` — иначе обходится
через консоль браузера.

### 7.2 Правит только своих

`canEdit(ch)` истинно, когда `ch.owner === player`. Чужой лист открыт на чтение,
кнопки правки погашены, сверху плашка «Персонаж игрока Гоша — только просмотр».

**Отдельная дверь для ЧП.** Кнопка «Править чужого персонажа» открывает диалог,
где нужно вписать причину. Только после этого лист разблокируется, и правка
уходит в журнал с пометкой `foreign: true` и текстом причины. Смысл: сделать
такую правку осознанной и заметной, а не невозможной — иначе в реальной игре
это просто обойдут, отредактировав JSON руками.

### 7.3 Журнал правок

**Новый файл `server/utils/audit.ts`** и обработчик, который пишет запись при каждом
`PUT` и `DELETE` персонажа. Журнал — `data/audit.log.jsonl`, по записи на строку.

Что в записи:

| Поле | Откуда | Зачем |
|---|---|---|
| `at` | время сервера, ISO | когда |
| `player` | выбранное имя | кто говорит, что он |
| `deviceId` | `localStorage`, не меняется при смене имени | **кто на самом деле** |
| `charId`, `charName`, `owner` | тело запроса | что правили |
| `foreign` | `owner !== player` | признак ЧП |
| `reason` | из диалога 7.2 | зачем |
| `changed` | список полей `было → стало` | что именно изменилось |
| `ua` | заголовок User-Agent | какой браузер и ОС |
| `ip` | `getRequestIP(event, { xForwardedFor: true })` | см. оговорку ниже |
| `host`, `osUser` | `os.hostname()`, `os.userInfo().username` | чья машина держит сервер |
| `prev` | HMAC предыдущей записи | цепочка, см. ниже |

**Про IP — честно.** Сейчас приложение работает на `localhost`, поэтому IP у всех
записей будет `127.0.0.1`, и пользы от него ноль. Поле пишем на будущее: оно
станет осмысленным в тот день, когда приложение выставят в локальную сеть или
на хостинг. Настоящий различитель сегодня — `deviceId`: он переживает смену имени
в селекте, поэтому если кто-то переключился на чужое имя, чтобы поправить лист,
в журнале это будет видно по совпадению устройства с его прежними записями.

### 7.4 Не шифрование, а цепочка хешей

**Почему не шифровать.** Задача сформулирована как «понять, кто правил чужого
персонажа, если случится ЧП». Это установление авторства и целостности, а не
секретность. Шифрование здесь не работает: ключ обязан лежать на той же машине,
где крутится приложение, а значит доступен тому же человеку. Зашифрованный журнал,
который расшифровывается кнопкой в том же приложении, — это украшение, а не защита.

**Что действительно решает задачу.** Каждая запись содержит HMAC-SHA256 от
предыдущей записи и собственного содержимого. Изменить или удалить старую строку,
не сломав цепочку, нельзя. Приложение при открытии журнала проверяет цепочку и
показывает «журнал цел» или «цепочка нарушена на записи N».

Ключ — `data/audit.key`, генерируется при первом запуске, **добавить в `.gitignore`**
(иначе он уедет в публичный репозиторий вместе с журналом, и цепочку сможет
переподписать кто угодно).

**Граница честности, записать в интерфейсе.** Тот, кто запускает сервер, держит
и ключ, и файл журнала, — он может переподписать всю цепочку заново. Это ловит
небрежность и правку по случайности, но не целенаправленную подделку. Настоящая
защита возможна только на сервере, который не контролируется игроками.

### 7.5 Куда это ведёт: git как настоящий журнал

Самый крепкий журнал в этом проекте уже существует — это история git. Каждое
изменение `characters/*.json` можно посмотреть с автором, временем и полным
диффом:

```bash
git log -p --follow characters/sema_shmold-mold_5iiifc7z.json
```

Сегодня у истории один недостаток: все коммиты подписаны `kondrieger`, потому что
пушит один человек. Лечится так: при сохранении персонажа приложение делает коммит
от имени **выбранного игрока**:

```bash
git -c user.name="Гоша" -c user.email="gosha@dolmenwood.local" commit -m "Гоша: правка листа Вэл" -- characters/nisya_vel_qhjq6txm.json
```

После этого история git становится основным журналом: она уже связана хешами по
построению, а после `push` в GitHub с защищённой веткой её нельзя переписать
незаметно — чего про локальный файл сказать нельзя.

Пункт необязательный и делается последним, после 7.1–7.4. Оговорка та же: имя
в коммите выбирает сам пользователь, так что это тоже про добросовестность.

### 7.6 Предупредить людей

Журнал пишет имя, браузер, устройство и (в будущем) IP семи живых людей. Это надо
сказать вслух, а не спрятать: строка в подсказке у селекта — «правки записываются
в журнал: кто, когда, с какого устройства». Игроки должны знать до того, как начнут
пользоваться, а не узнать при разборе ЧП.

---

## [ ] 8. Промпт портрета под конкретного персонажа

**Причина одинаковости.** `utils/portrait.js` собирает один и тот же каркас для всех:
меняется только строка `KINDRED_LOOK`, а стиль, свет, палитра и композиция
захардкожены одной строкой на всех. Класс в промпт попадает единственным словом.
Плюс баг: `HORN_BY_LEVEL[1]` подставлен жёстко, и у бреггла 10 уровня рога
описаны как однодюймовые у сошки.

**Переписать `utils/portrait.js`:**

1. **Внешность рода** — из описания рода в книге (стр. 32, 36, 40, 44, 48, 52):
   рост и телосложение из строк `Height` и `Weight`, кожа и шерсть, приметы вида.
   Мослинг — приземистый, кора и мох, белая сокоподобная кровь. Гримолкин — прямоходящий
   кот трёх футов. Вудгрю — уши-лопухи, лицо летучей мыши. И так далее.
2. **Облик класса или род-класса** — из страницы класса: что ему разрешено носить,
   что даёт стартовый набор, чем он узнаваем.
   Монах — тонзура и мешковинная ряса, без брони (стр. 66–67).
   Рыцарь — тяжёлые латы и герб **того самого выпавшего дома** (стр. 70–71).
   Маг — ритуальные одежды и книга заклинаний (стр. 72).
   Клирик — святой символ **своего ордена** (стр. 61).
   Охотник — кожа, лук, трофей (стр. 68–69).
   Вор — лёгкое и тёмное, без щита (стр. 74).
   Бард — инструмент (стр. 58). Чароплёт — мерцание гламуров (стр. 62).
3. **Сам персонаж** — все выпавшие приметы (голова, лицо, тело, шерсть, одежда, нрав,
   речь), прошлое, мировоззрение, лунный знак, симбиотическая плоть, сноровка,
   гламуры, безделушка, **настоящее носимое снаряжение**, уровень,
   и длина рогов бреггла **по фактическому уровню** из таблицы (стр. 34).
4. **Разнообразие с постоянством.** Палитра, освещение и композиция выбираются
   детерминированно из идентификатора персонажа: один и тот же герой всегда даёт
   один и тот же промпт, но два разных мослинга выглядят по-разному. Наборы —
   свои для смертных, фей и полуфейских (стр. 30–31: смертные земные и телесные,
   феи неземные и светящиеся, полуфейские между двух миров).
5. **Русская версия** — не подстрочник, а тот же смысл живым языком.

**Ручной ввод.** `buildCharacter` уже зовёт `portraitPrompt`, и `recompute` тоже.
Проверить, что блок `PortraitPrompt.vue` показывается на странице персонажа,
созданного вручную, и что кнопки копирования работают.

**Граница закона 1:** факты о персонаже — только из книги и кубиков.
Слова про палитру и манеру рисунка — оформление, их сочиняем свободно.

---

## Проверка перед завершением

- [x] сверочный скрипт: все таблицы предметов стр. 116–135 на месте, веса и цены
      сходятся — 401 проверка против текста книги;
- [ ] генерация всех сочетаний род+класс и всех пяти род-классов без ошибок;
- [ ] промпты двух мослингов одного класса заметно различаются, а один и тот же
      персонаж дважды даёт одинаковый промпт;
- [ ] оружие кладётся в рюкзак и возвращается на себя, вес сходится в обе стороны;
- [ ] поиск находит «веревку» и «верёвку», «кошель» и «кошёль»;
- [ ] без выбранного игрока ни одна кнопка правки не срабатывает;
- [ ] чужой персонаж открыт только на чтение; правка через дверь ЧП требует причины
      и попадает в журнал с пометкой `foreign`;
- [ ] запрос `PUT` в обход интерфейса (через консоль) отбивается сервером;
- [ ] подмена строки в середине `audit.log.jsonl` ломает цепочку, и приложение
      честно сообщает, на какой записи;
- [ ] `data/audit.key` не попал в git;
- [ ] перегенерация десять раз подряд не оставляет живых таймеров
      (проверить, что счётчик активных интервалов возвращается к нулю);
- [ ] пустая консоль на всех страницах: `/`, `/manual`, `/catalog`,
      `/characters/<id>`, `/rules`.
